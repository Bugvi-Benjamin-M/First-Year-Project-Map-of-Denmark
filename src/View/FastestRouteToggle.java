package View;

/**
 * The FastestRouteToggle is a visual component that handles the setting for
 * using the fastest route or the shortest route when searching for routes.
 */
public class FastestRouteToggle extends Toggle {

    private static final String TITLE = "Route Search - Find Fastest Route:";

    public FastestRouteToggle() {
        super(TITLE);
    }
}
